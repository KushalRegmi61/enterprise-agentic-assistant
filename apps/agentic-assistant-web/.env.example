# Base URL of the agentic-assistant backend (REST + /ask WebSocket).
# Local default is http://localhost:8000 (see services/agentic-assistant).
NEXT_PUBLIC_ASSISTANT_API_URL=http://localhost:8000

# Project-status MCP endpoint shown in generated coding-agent configuration.
NEXT_PUBLIC_PROJECT_MCP_URL=https://agentic-assistant-sha-42efdfb.onrender.com/mcp
